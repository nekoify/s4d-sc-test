const Discord = require('discord.js')
            
            module.exports = {
                name: new file lol,
                run: async (s4d, message) => {
                    
                }
            }